import React from 'react'
import ReactDOM from 'react-dom/client'
import VimTrainer from './VimTrainer.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VimTrainer />
  </React.StrictMode>,
)
